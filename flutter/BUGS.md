# Flutter 待修复问题 — 已关闭

> App B 改为 WebView 套壳 + 后台上报架构后，以下问题已不再需要修复。
> 全部 UI 功能直接使用 CF Workers 网页版，100% 一致。

## 已关闭（不再需要）

| 问题 | 状态 | 原因 |
|---|---|
| 1. 聊天字段名错误 | ✅ 已关闭 | UI 全部由 WebView 承载，Flutter 不维护独立 UI |
| 2. AI Provider 无 CF 选项 | ✅ 已关闭 | 同上 |
| 3. AiSummaryPage 空壳 | ✅ 已关闭 | 同上（已删除） |
| 4. 缺少 AI 用量显示 | ✅ 已关闭 | 同上 |
| 5. 外部模型列表拉取 | ✅ 已关闭 | 同上 |

## 当前 App B 架构

```
Flutter (8-12 MB 后台待机)
 ├── ReporterService (Isolate)  ← 静默上报
 └── WebView (按需加载)
      └── CF Workers 网页完整 UI
           ├── 设备卡片 / 日志 / 聊天
           ├── 历史记录 + 分页
           ├── AI 总结（含 CF 免费额度）
           └── 在线用户列表
```

## 内存策略

| 场景 | WebView | 内存 |
|---|---|
| 后台待机 | 销毁 | 8-12 MB |
| 用户打开 App | 创建并加载 CF 页面 | 临时 +25-35 MB |
| 用户切走 | 自动销毁 | 回到 8-12 MB |
