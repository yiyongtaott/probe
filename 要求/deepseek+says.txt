2026/06/06 - 任务完成报告

已更新以下 6 个文件：

1. lib/shared/models/models.dart
   - 新增 DeviceState, ChatMessage, OnlineUser, SyncPayload, HistoryResult, HeartbeatResult, AiSummaryResult, AiUsageResult
   - ActivityRecord 补充 fromJson() / toJson()
   - ReportPayload 保留原结构不变

2. lib/main_full.dart
   - 保留 Isolate.spawn 后台上报
   - 新增 ReceivePort/SendPort 隔离通信
   - 替换为 MaterialApp.router + GoRouter (StatefulShellRoute)
   - 5 个标签页: 概览 / 历史 / AI / 聊天 / 设置
   - 底部 NavigationBar 切换
   - 添加 Riverpod StateProvider

3. lib/main_reporter.dart
   - 替换两个 TODO 为完整实现
   - 新增 flutter_background_service 前台 Service
   - 每 60 秒 Timer 调用 reporter.onWake()
   - 保留 WorkManager ~15min 周期性任务

4. pubspec.yaml
   - 移除 cupertino_icons
   - 添加 flutter_markdown: ^0.7.0

5. lib/shared/background/reporter_service.dart
   - OfflineCache 改用 SharedPreferences 持久化
   - 新增 autoFlush() 方法 + 在 onWake() 中调用
   - 新增队列长度 Stream 供 UI 监听

6. lib/shared/api/report_client.dart
   - 已验证 Content-Type: application/json 已存在

额外创建的页面桩文件:
- lib/ui/pages/ai_summary_page.dart (新创建)
- lib/ui/pages/chat_page.dart (新创建)
- lib/ui/pages/settings_page.dart (新创建)
- dashboard_page.dart 和 history_page.dart 已存在完整实现，未改动
