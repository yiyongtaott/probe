name: tulpa_topic
description: "Tulpa Topic Engine - 浏览行为话题发动机"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # HTTP / AI API
  http: ^1.2.0

  # 本地存储
  sqflite: ^2.4.0
  path: ^1.9.0
  shared_preferences: ^2.3.0

  # 系统通知
  flutter_local_notifications: ^18.0.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
