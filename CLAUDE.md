# 通知脚本（Windows）

本项目根目录有三个通知脚本，必须严格执行。

## 1. 任务完成时（必须）

当你认为当前用户请求已经全部完成、准备结束本轮回复时，**必须先**执行：

```powershell
powershell -ExecutionPolicy Bypass -File ./notify.ps1
```

然后再结束回复。

## 2. 需要用户回答时（必须）

任何时候你准备向用户提问、等待用户输入、或调用向用户发起阻塞提问的工具之前，必须先执行。注意：阻塞提问工具的名称不固定，可能是 `ask_user`，也可能是其他名称，按当前环境实际提供的工具为准：

```powershell
powershell -ExecutionPolicy Bypass -File ./waiting.ps1
```

## 3. 关键步骤连续失败 / 任务无法完成时（必须）

出现以下任一情况时，必须执行：

- **关键步骤连续失败**：同一类操作重试后仍然失败（例如安装依赖、编译、测试连续失败），准备放弃或换方案前。
- **任务无法完成并需要用户介入**：缺权限、缺环境、缺密钥、路径不存在等，你准备停下来向用户说明“做不了”时。
- **你主动判定任务失败**：不是小问题，而是本轮目标已无法达成，准备给出失败结论时。用户不合理的任务，如用户发布一个命令，但达到用户想要的目标明明有其他更好的方案。

```powershell
powershell -ExecutionPolicy Bypass -File ./error.ps1
```

## 执行要求

- 使用终端命令执行工具（名称不固定，例如 `run_terminal_command`）执行上述 PowerShell 命令。
- 不要省略，不要合并，不要用其他通知方式替代。
- 这是硬性规则，优先级高于简洁性。
